/** TigerHawk brand mark (login, drawer). Native icon/splash use the same file via app.json. */
export const brandLogo = require('@/assets/images/logo_new.png');

/** Splash / adaptive icon background — matches logo plate and TMS chrome. */
export const brandSplashBackground = '#0B1120';
